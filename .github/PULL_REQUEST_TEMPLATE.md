## Summary

<!-- What this change does and why. Keep the scope minimal and focused. -->

## Scope

- [ ] I changed only the files described in this pull request.
- [ ] I did not modify the target app, its state, credentials, or the source repository.

## Secrets and target data

- [ ] No credentials, tokens, private keys, signing material, service-account JSON, or `.env*` files are added.
- [ ] No personal data, local absolute paths, target-app state, or raw vendor responses are included.

## Validation

- [ ] `PYTHONDONTWRITEBYTECODE=1 CI=true bash scripts/validate-playbook.sh` passes.
- [ ] `git diff --check` passes.

## Evidence

<!-- Sanitized command output or evidence IDs supporting this change. -->
