# VS Code / GitHub Copilot adapter

Inactive template: [templates/mcp.json](templates/mcp.json). It is not loaded from this repository.

After approval, copy it to the selected project's `.vscode/mcp.json`; VS Code supports workspace/project MCP configuration. Do not use this toolkit to update user settings. Use native browser OAuth for RevenueCat when VS Code offers it.

The XcodeBuildMCP entry runs persistent installed `xcodebuildmcp mcp`. Request explicit install and adapter-activation approval first; it never downloads at activation. Discover live tool schemas before use. Never add secrets to this template.

Return to [the canonical skill](../../skills/mobile-app-ship/SKILL.md).
