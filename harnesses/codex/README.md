# OpenAI Codex CLI adapter

Inactive template: [templates/config.toml](templates/config.toml). It is not loaded from this repository.

After approval, merge the two `mcp_servers` entries into the selected project's `.codex/config.toml`; project-local config is supported. Do not copy this toolkit file into user/global Codex configuration. Use native browser OAuth for RevenueCat when Codex offers it.

The XcodeBuildMCP entry runs persistent installed `xcodebuildmcp mcp`. Request explicit install and adapter-activation approval first; it never downloads at activation. Discover live tool schemas before use. Never add secrets to this template.

Return to [the canonical skill](../../skills/mobile-app-ship/SKILL.md).
