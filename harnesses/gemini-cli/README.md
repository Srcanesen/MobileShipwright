# Gemini CLI adapter

Inactive template: [templates/settings.json](templates/settings.json). It is not loaded from this repository.

After approval, copy it only when the selected project's `.gemini/settings.json` does not exist; otherwise merge it manually. Gemini CLI has no native skill path: do not copy a fake skill. An explicitly approved `GEMINI.md` manual-context fallback is the only package fallback. Do not use this toolkit to modify user/global settings. Use native browser OAuth for RevenueCat when Gemini CLI offers it.

The XcodeBuildMCP entry runs persistent installed `xcodebuildmcp mcp`. Request explicit install and adapter-activation approval first; it never downloads at activation. Discover live tool schemas before use. Never add secrets to this template.

Return to [the canonical skill](../../skills/mobile-app-ship/SKILL.md).
